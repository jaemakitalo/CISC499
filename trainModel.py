
#Train final model to minimize maximum per-cluster risk
#Input: data (sorted), cluster labels
def trainModel (sortedData, cluster):

	return newFeaturizer, linearClassificationHead
    # parameterized featurizer & logit(training score/accuracy)
